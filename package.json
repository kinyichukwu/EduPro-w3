{
  "name": "edupro",
  "version": "1.0.0",
  "description": "EduPro - Educational Platform with React Frontend and Go Backend",
  "scripts": {
    "dev": "concurrently \"cd backend && go run main.go\" \"cd frontend && yarn dev\"",
    "dev:frontend": "cd frontend && yarn dev",
    "dev:backend": "cd backend && go run main.go",
    "build": "cd frontend && yarn build",
    "build:backend": "cd backend && go build -o edupro-backend main.go"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "private": true
}
